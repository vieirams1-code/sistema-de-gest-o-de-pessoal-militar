| Arquivo | Módulo | Permissão encontrada | Tipo de uso | Dependências | Risco | Recomendação |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| src/App.jsx | Geral | RequireAdmin / RequireModuleAccess | Rota | pages.config.js | Rotas sem proteção explícita podem ser acessadas via URL direta. | Adicionar todas as páginas sensíveis a `adminOnlyPages` ou `moduleGuardByPage`. |
| src/App.jsx | Backup do Sistema | - | Rota | - | **ALTO**: Página de Backup não possui guard de rota, apesar de ser adminOnly no menu. | Adicionar 'BackupSistema' em `adminOnlyPages`. |
| src/Layout.jsx | Geral | adminOnly, viewPermission, actionKey, moduleKey | Menu | useCurrentUser | Inconsistência entre visibilidade do menu e proteção da rota. | Sincronizar logicamente o menu com os guards do `App.jsx`. |
| src/pages/Ferias.jsx | Férias | admin_mode, excluir_ferias | Botão / Admin | modoAdmin (state) | Lógica de "Modo Admin" local pode ser confusa e permite bypass se o state for forçado. | Centralizar validação de ações críticas no backend (já iniciado via cudEscopado). |
| src/pages/Publicacoes.jsx | Publicações | admin_mode, excluir_publicacoes, publicar_bg | Botão / Admin | modoAdmin (state) | Uso de 'admin_mode' para sobrescrever permissões específicas. | Padronizar o uso de 'admin_mode' como uma permissão global de supervisor. |
| src/services/cudEscopadoClient.js | Geral | Allowlist de entidades | Backend | Deno: cudEscopado | Backend hardening parcial; entidades fora da lista usam SDK direto sem validação de escopo. | Expandir allowlist para todas as entidades que possuem campo `militar_id` ou `unidade_id`. |
| src/services/acervoHistoricoService.js | Acervo Histórico | - | Backend | base44.entities | **MÉDIO**: Realiza update direto em `AcervoFuncionalHistorico` ignorando `cudEscopado`. | Migrar para `atualizarEscopado` para garantir validação de escopo no backend. |
| src/services/importacaoMedalhaService.js | Medalhas | - | Backend | base44.entities | **MÉDIO**: Cria `PublicacaoExOfficio` diretamente via SDK. | Migrar para `criarEscopado` para assegurar integridade do escopo militar. |
| src/components/auth/RequireModuleAccess.jsx | Auth | permissions === 'ALL' | UI | useCurrentUser | Dependência de sentinela 'ALL' legado; dificulta auditoria granular. | Substituir checagem de 'ALL' por `isAdmin` ou `canAccessAll` provenientes do hook. |
| src/components/auth/useCurrentUser.jsx | Auth | isAdmin, hasAccess, getMilitarScopeFilters | Escopo | getUserPermissions (Deno) | Centralização positiva, mas complexidade no fallback de escopo organizacional. | Simplificar a resolução de escopo delegando totalmente ao backend via `scope` object. |
| src/pages/RegistrosMilitar.jsx | Registros | admin_mode | Botão | isAdmin | 'admin_mode' verificado diretamente via actionKey sem interface de ativação clara. | Padronizar UI de ativação do modo admin. |
| src/lib/PageNotFound.jsx | Geral | role === 'admin' | UI | authData.user | Uso de 'role' legado em vez de permissões consolidadas. | Migrar para `isAdmin` do hook `useCurrentUser`. |
| src/services/comportamentoService.js | Comportamento | permissions === 'ALL' | Backend (logic) | usuarioAtual | Bypass total de lógica para usuários com permissão 'ALL'. | Refatorar para verificar ações específicas (ex: 'visualizar_todos_comportamentos'). |
